// const runSpawn = require("");
// const calculateCreepsToSpawn = require("");

// const runRoomSpawns = (room) => {
// 	calculateCreepsToSpawn(room);

// 	let spawns = room.find(FIND_MY_STRUCTURES, {
// 		filter: { structureType: STRUCTURE_SPAWN },
// 	});

// 	if (spawns) {
// 		spawns.forEach((spawn) => {
// 			runSpawn(spawn);
// 		});
// 	}
// };

// module.exports = runRoomSpawns;
